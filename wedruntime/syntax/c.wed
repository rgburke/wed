syntax {
    name = "c";
    
    pattern {
        regex = /asm|break|case|continue|default|do|else|enum|for|fortran|goto|if|return|struct|switch|typedef|union|while/;
        type = "keyword";
    }
}
